@model List<Portal.Core.ViewModel.GLMainGroupViewModel>
<script src="~/Scripts/jquery.tablesorter.js"></script>
@if (Model != null && Model.Count > 0)
{
    <div style="overflow-x:scroll; padding:10px;">
        <table class="table" id="tblGLMainGroupList">
            <thead>
                <tr>
                    <th>GL Main Group Name</th>
                    <th>GL Type</th>
                    <th>SequenceNo</th> 
                    <th>Created By User</th>
                    <th>Created Date</th>
                    <th>Status</th>
                    <th colspan="2" align="center">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="success">
                        <td>@item.GLMainGroupName</td>
                        <td>@item.GLType</td>
                        <td>@item.SequenceNo</td>
                        <td>@item.CreatedName</td>
                        <td>@item.CreatedDate</td>
                        <td>@(item.GLMainGroup_Status == true ? "Active" : "Inactive")</td>
                        <td class="btn btn-success table_btn" style="width:30px; margin:0px auto;">
                            <a style="color:white" class="fa fa-eye" aria-hidden="true" href="~/GLMainGroup/AddEditGLMainGroup?GLMainGroupId=@item.GLMainGroupId&AccessMode=3"></a>
                        </td>
                        <td class="btn btn-success table_btn" style="width:30px; margin:0px 2px auto;">
                            <a style="color:white" class="fa fa-pencil-square-o" href="~/GLMainGroup/AddEditGLMainGroup?GLMainGroupId=@item.GLMainGroupId&AccessMode=2"></a>
                        </td>

                    </tr>
                }

            </tbody>
        </table>
    </div>
}
else
{
    <h3 style="color:firebrick;text-align:center">Records not Found!!!</h3>
}
<script>
    $(document).ready(function () {
        $('#tblGLMainGroupList').paging({
            limit: 10,
            rowDisplayStyle: 'block',
            activePage: 0,
            rows: []
        });

    });
</script>