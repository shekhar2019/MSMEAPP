@model List<Portal.Core.ViewModel.PurchaseSummaryRegisterViewModel>
<script src="~/Scripts/jquery.tablesorter.js"></script>
<style>
.table {
    width: 109% !important;
    max-width: 109%  !important;
    margin-bottom: 20px;
}
</style>
@if (Model != null && Model.Count > 0)
{
    <div style="overflow-x:scroll; padding:10px;">
        <table class="table" id="tblPurchaseInvoiceList">
            <thead>
                <tr>
                    <th>Invoice No.</th>
                    <th>Invoice Date</th> 
                    <th>Vendor Name</th>
                    <th>State</th>
                    <th>Gross Amount</th> 
                    <th>Total Amount</th>
                    <th>Amount Paid</th>
                    <th>Amount Pending</th>
                    <th>Created By User</th>
                    
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="success">
                        <td style="margin:0px 2px auto;">
                            @item.InvoiceNo
                        </td> 
                        <td>@item.InvoiceDate</td> 
                        <td>@item.VendorName</td>
                        <td>@item.StateName</td>
                        <td>@item.BasicValue</td> 
                        <td>@item.TotalValue</td>
                        <td>@item.AmountPaid</td>
                        <td>@item.AmountPending</td>
                        <td>@item.CreatedByUserName</td>
                        
  </tr>
                }

            </tbody>
        </table>
    </div>
}
else
{
    <h3 style="color:firebrick;text-align:center">Records not Found!!!</h3>
}
<script>
    $(document).ready(function () {
        $('#tblPurchaseInvoiceList').paging({
            limit: 100,
            rowDisplayStyle: 'block',
            activePage: 0,
            rows: []
        });

    });
</script>