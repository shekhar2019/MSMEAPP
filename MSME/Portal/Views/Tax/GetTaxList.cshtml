@model List<Portal.Core.ViewModel.TaxViewModel>
<script src="~/Scripts/jquery.tablesorter.js"></script>
@if (Model != null && Model.Count > 0)
{
    <div style="overflow-x:scroll; padding:10px;">
        <table class="table" id="tblTaxList">
            <thead>
                <tr>
                    <th>Tax Name</th>
                    <th>Tax Type</th>   
                    <th>Tax Sub Type</th>  
                    <th>Tax Percentage</th>   
                    <th>Form Type Desc</th>   
                    <th>Create By</th>  
                    <th>Created Date</th> 
                    <th>Modified By</th>
                    <th>Modified Date</th> 
                    <th>Tax Status</th>
                    <th colspan="2" align="center">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="success">
                        <td>@item.TaxName</td>
                        <td>@item.TaxType</td>  
                        <td>@item.TaxSubType</td>  
                        <td>@item.TaxPercentage</td>   
                        <td>@item.FormTypeDesc</td>   
                        <td>@item.CreatedName</td>
                        <td>@item.CreatedDate</td>  
                        <td>@item.ModifiedName</td>
                        <td>@item.ModifiedDate</td> 
                        <td>@(item.Tax_Status == true ? "Active" : "Inactive")</td>
                        <td class="btn btn-success table_btn" style="width:30px; margin:0px auto;">
                            <a style="color:white" class="fa fa-eye" aria-hidden="true" href="~/Tax/AddEditTax?TaxId=@item.TaxId&AccessMode=3"></a>
                        </td>
                        <td class="btn btn-success table_btn" style="width:30px; margin:0px 2px auto;">
                            <a style="color:white" class="fa fa-pencil-square-o" href="~/Tax/AddEditTax?TaxId=@item.TaxId&AccessMode=2"></a>
                        </td> 
                    </tr>
                }

            </tbody>
        </table>
    </div>
}
else
{
    <h3 style="color:firebrick;text-align:center">Records not Found!!!</h3>
}
<script>
    $(document).ready(function () {
        $('#tblTaxList').paging({
            limit: 10,
            rowDisplayStyle: 'block',
            activePage: 0,
            rows: []
        });

    });
</script>